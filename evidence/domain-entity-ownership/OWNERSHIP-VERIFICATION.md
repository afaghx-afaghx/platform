# Ownership Verification Evidence

This directory is the machine-readable evidence boundary for G05.1.

CI generates `ownership-verification.json` by executing the ownership verifier. The committed JSON is only a contract/placeholder and must never be treated as a verification result.
