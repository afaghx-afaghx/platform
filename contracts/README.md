# Contracts

This directory is the canonical home for externally meaningful contracts.

- `openapi/` — versioned HTTP API contracts.
- `events/` — asynchronous event envelopes and schemas.
- `schemas/` — shared schemas that have explicit ownership.

Contracts are not a dumping ground for internal domain models. Every contract has an owner and compatibility expectations.
