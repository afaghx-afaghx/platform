# Route validation script

## Purpose
Validate the machine-readable route ownership registry.

## Usage
```sh
./routes/scripts/validate.sh
```

Checks JSON validity, required ownership/security fields, allowed layers, the Experience database-path pattern, and the canonical request flow.
