# Domain Boundary

Business capabilities are independently bounded contexts. Each domain owns its business rules and persistence boundary.

Cross-domain collaboration uses versioned contracts, events, or explicit application interfaces. Direct cross-domain table access is forbidden.
