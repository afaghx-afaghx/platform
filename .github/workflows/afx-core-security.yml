name: AFX-CORE Security

on:
  push:
    paths:
      - 'core/AFX-CORE/**'
      - 'docs/security/**'
      - '.github/workflows/afx-core-security.yml'
  pull_request:
    paths:
      - 'core/AFX-CORE/**'
      - 'docs/security/**'
      - '.github/workflows/afx-core-security.yml'

permissions:
  contents: read

jobs:
  security-tests:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: afxcore
          POSTGRES_PASSWORD: afxcore
          POSTGRES_DB: afxcore_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U afxcore -d afxcore_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: core/AFX-CORE/package-lock.json
      - name: Install locked dependencies
        working-directory: core/AFX-CORE
        run: npm ci --no-audit --no-fund
      - name: Verify dependency tree
        working-directory: core/AFX-CORE
        run: npm ls --all
      - name: Run AFX-CORE bootstrap security tests
        working-directory: core/AFX-CORE
        run: npm run test:security 2>&1 | tee ../../artifacts-security-bootstrap.txt
      - name: Run G01-11 HTTP/API security boundary integration tests
        working-directory: core/AFX-CORE
        run: node --test test/http-security.test.js 2>&1 | tee ../../artifacts-security-http-api.txt
      - name: Run AFX-CORE PostgreSQL persistence and refresh race tests
        working-directory: core/AFX-CORE
        env:
          DATABASE_URL: postgresql://afxcore:afxcore@localhost:5432/afxcore_test
        run: npm run test:persistence 2>&1 | tee ../../artifacts-security-persistence.txt
      - name: Run G01-12 dedicated concurrency security tests
        working-directory: core/AFX-CORE
        env:
          DATABASE_URL: postgresql://afxcore:afxcore@localhost:5432/afxcore_test
        run: npm run test:g01-12 2>&1 | tee ../../artifacts-security-g01-12.txt
      - name: Run G01-13 password hashing calibration
        working-directory: core/AFX-CORE
        run: npm run test:g01-13 2>&1 | tee ../../artifacts-security-g01-13.txt
      - name: Capture PostgreSQL evidence
        if: always()
        env:
          PGPASSWORD: afxcore
        run: |
          pg_isready -h localhost -p 5432 -U afxcore -d afxcore_test | tee artifacts-security-postgres-health.txt
          psql -h localhost -p 5432 -U afxcore -d afxcore_test -c 'SELECT version();' | tee artifacts-security-postgres-version.txt
          psql -h localhost -p 5432 -U afxcore -d afxcore_test -c "SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name LIKE 'afx_%' ORDER BY table_name;" | tee artifacts-security-schema-tables.txt
      - name: Assemble security evidence artifact
        if: always()
        run: |
          mkdir -p artifacts/afx-core-security
          cp artifacts-security-bootstrap.txt artifacts/afx-core-security/ 2>/dev/null || true
          cp artifacts-security-http-api.txt artifacts/afx-core-security/ 2>/dev/null || true
          cp artifacts-security-persistence.txt artifacts/afx-core-security/ 2>/dev/null || true
          cp artifacts-security-g01-12.txt artifacts/afx-core-security/ 2>/dev/null || true
          cp artifacts-security-g01-13.txt artifacts/afx-core-security/ 2>/dev/null || true
          cp artifacts-security-postgres-health.txt artifacts/afx-core-security/ 2>/dev/null || true
          cp artifacts-security-postgres-version.txt artifacts/afx-core-security/ 2>/dev/null || true
          cp artifacts-security-schema-tables.txt artifacts/afx-core-security/ 2>/dev/null || true
          {
            echo '# AFX-CORE Security CI Evidence'
            echo
            echo "Commit: $GITHUB_SHA"
            echo "Workflow: $GITHUB_WORKFLOW"
            echo "Run: $GITHUB_RUN_ID"
            echo "Node: $(node --version 2>/dev/null || true)"
            echo
            echo 'Controls exercised:'
            echo '- Deterministic npm dependency installation via package-lock.json + npm ci'
            echo '- G01-11 HTTP/API authentication and authorization boundary integration tests'
            echo '- PostgreSQL 16 service health and schema evidence'
            echo '- Durable PostgreSQL-backed identity/membership/session persistence'
            echo '- Transactional refresh-token rotation with row-level locking'
            echo '- G01-12 concurrent refresh race: exactly one winner'
            echo '- G01-12 refresh-family and session revocation after reuse detection'
            echo '- G01-13 password hashing latency, memory and concurrency calibration evidence'
          } > artifacts/afx-core-security/README.md
      - name: Upload security evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: afx-core-security-evidence-${{ github.run_id }}
          path: artifacts/afx-core-security
