# AFX-PLATFORM Boundary

Shared technical capabilities live here: API, Gateway, Events, Queue, Workflow, Search, Cache, Storage, Notification, Webhooks, Scheduler, Integration, Localization, Currency and Documents.

Platform capabilities are exposed through explicit interfaces and contracts. The platform must not absorb business-domain rules.
