# Security Tests

Cross-cutting security verification for fail-closed behavior, tenant isolation, credential/token handling, audit-sensitive paths, and boundary enforcement.

Implementation authority remains with AFX-CORE and the owning layer. These tests must not create a parallel security subsystem.
