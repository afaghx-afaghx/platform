# End-to-End Tests

Verification of complete user-facing flows across Experience, Platform, Domain, and Core boundaries.

E2E tests must respect the canonical API boundary and must never access the database directly from Experience.
