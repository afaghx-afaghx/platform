# Test Evidence
Evidence is machine-readable output from actual test execution. The collector does not invent PASS/GREEN states. A report is valid only when it contains an execution timestamp, commit reference, suite result, and observed counts.
