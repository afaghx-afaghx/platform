# Intentionally non-production example.
# Application images must be added only after their canonical runtime/build
# contracts exist. Do not treat this file as an application service image.
FROM node:22-alpine
WORKDIR /workspace
